// autogenerated: do not edit!
// generated from gentemplate [gentemplate -d Package=elib -id Uint64 -d VecType=Uint64Vec -d Type=uint64 vec.tmpl]

// Copyright 2016 Platina Systems, Inc. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

package elib

type Uint64Vec []uint64

func (p *Uint64Vec) Resize(n uint) {
	old_cap := uint(cap(*p))
	new_len := uint(len(*p)) + n
	if new_len > old_cap {
		new_cap := NextResizeCap(new_len)
		q := make([]uint64, new_len, new_cap)
		copy(q, *p)
		*p = q
	}
	*p = (*p)[:new_len]
}

func (p *Uint64Vec) validate(new_len uint, zero uint64) *uint64 {
	old_cap := uint(cap(*p))
	old_len := uint(len(*p))
	if new_len <= old_cap {
		// Need to reslice to larger length?
		if new_len > old_len {
			*p = (*p)[:new_len]
			for i := old_len; i < new_len; i++ {
				(*p)[i] = zero
			}
		}
		return &(*p)[new_len-1]
	}
	return p.validateSlowPath(zero, old_cap, new_len, old_len)
}

func (p *Uint64Vec) validateSlowPath(zero uint64, old_cap, new_len, old_len uint) *uint64 {
	if new_len > old_cap {
		new_cap := NextResizeCap(new_len)
		q := make([]uint64, new_cap, new_cap)
		copy(q, *p)
		for i := old_len; i < new_cap; i++ {
			q[i] = zero
		}
		*p = q[:new_len]
	}
	if new_len > old_len {
		*p = (*p)[:new_len]
	}
	return &(*p)[new_len-1]
}

func (p *Uint64Vec) Validate(i uint) *uint64 {
	var zero uint64
	return p.validate(i+1, zero)
}

func (p *Uint64Vec) ValidateInit(i uint, zero uint64) *uint64 {
	return p.validate(i+1, zero)
}

func (p *Uint64Vec) ValidateLen(l uint) (v *uint64) {
	if l > 0 {
		var zero uint64
		v = p.validate(l, zero)
	}
	return
}

func (p *Uint64Vec) ValidateLenInit(l uint, zero uint64) (v *uint64) {
	if l > 0 {
		v = p.validate(l, zero)
	}
	return
}

func (p *Uint64Vec) ResetLen() {
	if *p != nil {
		*p = (*p)[:0]
	}
}

func (p Uint64Vec) Len() uint { return uint(len(p)) }
